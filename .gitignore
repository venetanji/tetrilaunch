# dependencies
node_modules/
app/node_modules/

# build output
app/dist/
dist/

# wrangler / cloudflare
.wrangler/
.dev.vars

# capacitor native platforms
# app/ios/ IS committed — it carries the Xcode project, Info.plist tweaks and
# app icons, so a Mac only needs `pod install` to build. Its own
# app/ios/.gitignore excludes Pods/, build output and the copied web assets.
# Android is still generated on demand with `npx cap add android`.
app/android/

# misc
*.log
.DS_Store
.vscode/
__pycache__/
